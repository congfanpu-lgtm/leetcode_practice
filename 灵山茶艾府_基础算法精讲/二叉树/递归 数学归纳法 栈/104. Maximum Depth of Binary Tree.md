**刷题日期**：2026-1-21
**难度**：Easy

## 题目截图：
---
![](assets/104.%20Maximum%20Depth%20of%20Binary%20Tree/file-20260123164712609.png)

## 参考答案：
---
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
    ## 递归
        if root is None: ## 边界条件
            return 0
        l_depth = self.maxDepth(root.left)
        r_depth = self.maxDepth(root.right)
        return max(l_depth,r_depth) + 1
```
**注意**：
1. 题目定义了maxDepth，下面一直循环递归
2. 递归运用到了栈，最后进去的最先出来

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right

class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
    ## 全局变量法
        ans = 0
        def f(node,cnt):
            if node is None:
                return
            cnt += 1
            nonlocal ans
            ans = max(ans,cnt)
            f(node.left,cnt)
            f(node.right,cnt)
        f(root,0)
        return ans
```

## 解题思路：
---
递归：

![](assets/二叉树思路及不同题型/file-20260123171619464.png)
![](assets/二叉树思路及不同题型/file-20260123171619463.png)
二叉树的边界条件是空节点，我们可以直接返回0作为答案
![](assets/二叉树思路及不同题型/file-20260123171619460.png)
1. 二叉树要先找边界条件
2. 关键在于return，你想给上一个节点什么信息
