**刷题日期**：2026-1-25
**难度**：Medium

## 题目截图：
---
![](assets/513.%20Find%20Bottom%20Left%20Tree%20Value/file-20260125224707462.png)


## 参考答案：
---
```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def findBottomLeftValue(self, root: Optional[TreeNode]) -> int:
        q = deque([root])
        while q:
            node = q.popleft()
            if node.right: q.append(node.right)
            if node.left: q.append(node.left)
        return node.val
```
**思路**：
1. 每一层从右往左遍历，这样最后一个node.val就是要的答案