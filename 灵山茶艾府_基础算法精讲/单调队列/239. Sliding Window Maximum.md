**刷题日期**：2026-2-19
**难度**：Medium
## 题目截图：
---
![](assets/239.%20Sliding%20Window%20Maximum/file-20260219150144732.png)

## 参考答案：
---
```python
class Solution:
    def maxSlidingWindow(self, nums: List[int], k: int) -> List[int]:
        # U = len(set(nums))
        # O(min(k,U))
        ans =[]
        q = deque()
        for i, x in enumerate(nums):
            # 1.入
            while q and nums[q[-1]] <= x:
                q.pop()
            q.append(i)
            # 2.出
            if i-q[0] >= k:
                q.popleft()
            # 3.记录答案
            if i >= k-1:
                ans.append(nums[q[0]])
        return ans
```
## 解题思路：
---
![](assets/239.%20Sliding%20Window%20Maximum/file-20260219150144734.png)
![](assets/239.%20Sliding%20Window%20Maximum/file-20260219150144733.png)
![](assets/239.%20Sliding%20Window%20Maximum/file-20260219150657373.png)
从「维护单调性」的角度上来说，单调队列和单调栈是一样的，一个弹出队尾元素，另一个弹出栈顶元素。在单调栈的基础上，单调队列多了一个「移除队首」的操作，这类似滑动窗口移动左指针 left 的过程。所以从某种程度上来说，单调队列 = 单调栈 + 滑动窗口。
