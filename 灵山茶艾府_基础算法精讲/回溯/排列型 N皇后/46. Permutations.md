**刷题日期**：2026-1-23
**难度**：Medium

## 题目截图：
---
![](assets/46.%20Permutations/file-20260128084403537.png)

## 参考答案：
---
做法一（用数组记录选了那些数字，用集合记录哪些还可以选）：
```python
class Solution:
    def permute(self, nums: List[int]) -> List[List[int]]:
        n = len(nums)
        ans = []
        path = [0] * n ## 也可以开一个空数组下面用append和pop来维护，两种写法都可以
        def dfs(i,s):
            if i == n:
                ans.append(path.copy()) # 如果不copy路径后期修改path 会修改到ans里面的数组
                return
            for x in s: ## 循环保证每一层都能把所有可能结果找到
                path[i] = x
                dfs(i+1, s-{x})
        dfs(0, set(nums))
        return ans
```
时间复杂度：O(n⋅n!)，其中 n 为 nums 的长度。视频中提到，搜索树中的节点个数低于 3⋅n!。实际上，精确值为 ⌊e⋅n!⌋，其中 e=2.718⋯ 为自然常数。有 O(n!) 个叶节点，每个叶节点花费 O(n) 的时间复制 path 数组，因此时间复杂度为 O(n⋅n!）

做法二：
```python
class Solution:
    def permute(self, nums: List[int]) -> List[List[int]]:
        n = len(nums)
        ans = []
        path = [0] * n ## 也可以开一个空数组下面用append和pop来维护，两种写法都可以
        on_path = [False] * n
        def dfs(i):
            if i == n:
                ans.append(path.copy())
                return
            for j in range(n):
                if not on_path[j]:
                    path[i] = nums[j]
                    on_path[j] = True
                    dfs(i+1)
                    on_path[j] = False # 恢复现场
        dfs(0)
        return ans
```
## 解题思路：
---
### 做法一
![](assets/46.%20Permutations/file-20260128084403543.png)
![](assets/46.%20Permutations/file-20260128084403540.png)

### 做法二：
![](assets/46.%20Permutations/file-20260128084525806.png)
## 时间复杂度:
---
知道节点个数就知道了递归的次数
![](assets/46.%20Permutations/file-20260128084403536.png)
1. 通过两种方式可以计算节点个数，通过图片右上角公式，在后面将极限等于e的公式补齐（括号内* 3!大于1，括号后的小于1，因此可以通过向下取整得到结果），然后向下取整，即可得到，e* n!下取整为节点个数 O(n!)
2. 通过放缩，(左边栏)小于3* n!    O(n!)
时间复杂度=叶子个数 * 从根到叶子的路径长度来算 O(n * n!)  
          =节点个数 * copy的时间 O(n * n!)  
![](assets/46.%20Permutations/file-20260128084534847.png)
