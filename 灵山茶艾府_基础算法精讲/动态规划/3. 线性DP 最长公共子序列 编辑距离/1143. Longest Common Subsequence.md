**刷题日期**：2026-2-3
**难度**：Medium

## 题目截图：
---
![](assets/1143.%20Longest%20Common%20Subsequence/file-20260203111413097.png)

## 参考答案：
---
递归：
```python
class Solution:
    def longestCommonSubsequence(self, text1: str, text2: str) -> int:
        n = len(text1)
        m = len(text2)
  
        @cache
        def dfs(i,j):
            if i < 0 or j < 0:
                return 0
            if text1[i] == text2[j]:
                return dfs(i-1, j-1) + 1
            return max(dfs(i-1,j), dfs(i, j-1))
        return dfs(n-1, m-1)
```
时间复杂度O(nm) 状态个数* 单个状态的计算时间

递推：
```python
class Solution:
    def longestCommonSubsequence(self, text1: str, text2: str) -> int:
        n = len(text1)
        m = len(text2)
  
        f = [[0] * (m + 1) for _  in range(n+1)]
        for i, x in enumerate(text1):
            for j, y in enumerate(text2):
                if x == y:
                    f[i+1][j+1] = f[i][j] + 1
                else:
                    f[i+1][j+1] = max(f[i][j+1], f[i+1][j])
        return f[n][m]
```

递推+ 优化空间复杂度: 空间复杂度O(m)
```python
class Solution:
    def longestCommonSubsequence(self, text1: str, text2: str) -> int:
        n = len(text1)
        m = len(text2)
  
        f = [0] * (m+1)
        for i, x in enumerate(text1):
            pre = f[0]
            for j, y in enumerate(text2):
                tmp = f[j+1]
                if x == y:
                    f[j+1] = pre + 1
                else:
                    f[j+1] = max(f[j+1], f[j])
                pre = tmp
        return f[m]
```
## 解题思路：
---
![](assets/1143.%20Longest%20Common%20Subsequence/file-20260203111413102.png)
从右往左的递归方便1:1翻译成从左往右的递推
![](assets/1143.%20Longest%20Common%20Subsequence/file-20260203111413101%201.png)
思考在第一种情况下需要考虑只选其中一个的情况吗
在第二种情况下需要考虑都不选的情况吗
都不需要![](assets/1143.%20Longest%20Common%20Subsequence/file-20260203111413101.png)
可以简化为：
![](assets/1143.%20Longest%20Common%20Subsequence/file-20260203111413100.png)
改为递推:
![](assets/1143.%20Longest%20Common%20Subsequence/file-20260203112130075.png)