**刷题日期**：2026-1-21
**难度**：Medium

## 题目截图
---
![](assets/82.%20Remove%20Duplicates%20from%20Sorted%20List%20Il/file-20260121145317668.png)

## 回答：
---
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        dummy = ListNode(next=head)
        left = dummy
        mid = None
        right = head
        while right:
            if right == right.next:
                mid = right
                right = right.next
            else:
                left =
```
**注意**：回答错误，绕不出来了

## 参考答案：
---
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next

class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        dummy = ListNode(next=head)
        cur = dummy

        while cur.next and cur.next.next:
            val = cur.next.val
            if cur.next.next.val == val:
                while cur.next and cur.next.val == val:
                    cur.next = cur.next.next
            else:
                cur = cur.next
        return dummy.next
```
**注意事项**：
1. 每次查看后面两个值，如果相同循环删除（这个循环够我学了，给到夯爆了）
2. while的判断条件是cur.next and cur.next.next
## 题目解析：
---
![](assets/82.%20Remove%20Duplicates%20from%20Sorted%20List%20Il/file-20260121145424976.png)
每次循环看下一个节点和下下一个节点的值是否一样，如果一样再套一个循环不断删除节点，直到没有节点或者遇到的节点值不一样
![](assets/82.%20Remove%20Duplicates%20from%20Sorted%20List%20Il/file-20260121145550696.png)
如果后面这两个值不一样，那么cur就移动到下一个节点（和我的思路一样，但是不知道怎么用代码实现）