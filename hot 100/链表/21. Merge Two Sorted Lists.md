**刷题日期**：2026-2-25
**难度**：Medium

## 题目截图：
---
![](assets/21.%20Merge%20Two%20Sorted%20Lists/file-20260225172921102.png)
## 参考答案：
---
```python
class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        cur = dummy = ListNode()  # 用哨兵节点简化代码逻辑
        while list1 and list2:
            if list1.val < list2.val:
                cur.next = list1  # 把 list1 加到新链表中
                list1 = list1.next
            else:  # 注：相等的情况加哪个节点都是可以的
                cur.next = list2  # 把 list2 加到新链表中
                list2 = list2.next
            cur = cur.next
        cur.next = list1 or list2  # 拼接剩余链表
        return dummy.next
```
**注意**：
1. 运用python短路求值特性：cur.next = list1 or list2：
```python
cur.next = list1 or list2
# 等价于：
if list1:           # 如果list1不为空
    cur.next = list1
else:               # 如果list1为空
    cur.next = list2
```
2. cur在进行链表拼接的时候跑到后面去了，因此需要用dummy来return结果