**刷题日期**：2026-2-25
**难度**：Medium

## 题目截图：
---
![](assets/24.%20Swap%20Nodes%20in%20Pairs/file-20260225211603904.png)

## 回答（正确）：
----
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def switchTwo(self, head:Optional[ListNode]) -> Optional[ListNode]:
        pre = None
        cur = head
        for _ in range(2):
            nxt = cur.next
            cur.next = pre
            pre = cur
            cur = nxt
        return pre
  
    def swapPairs(self, head: Optional[ListNode]) -> Optional[ListNode]:
        prev = head1 = ListNode()
        if head is None:
            return head
        while head and head.next :
            head3 = head.next.next
            head1.next = self.switchTwo(head)
            head = head3
            head1 = head1.next.next
        if head:
            head1.next = head
        return prev.next
```

## 参考答案：
---
**方法一 迭代：**
```python
class Solution:
    def swapPairs(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node0 = dummy = ListNode(next=head)  # 用哨兵节点简化代码逻辑
        node1 = head
        while node1 and node1.next:  # 至少有两个节点
            node2 = node1.next
            node3 = node2.next
  
            node0.next = node2  # 0 -> 2
            node2.next = node1  # 2 -> 1
            node1.next = node3  # 1 -> 3
  
            node0 = node1  # 下一轮交换，0 是 1
            node1 = node3  # 下一轮交换，1 是 3
        return dummy.next  # 返回新链表的头节点
```
时间复杂度O(n)，空间复杂度O(1)
![](assets/24.%20Swap%20Nodes%20in%20Pairs/file-20260225211805223.png)

**方法二 递归**：
```python
class Solution:
    def swapPairs(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if head is None or head.next is None:  # 递归边界
            return head  # 不足两个节点，无需交换
  
        node1 = head
        node2 = head.next
        node3 = node2.next
  
        node1.next = self.swapPairs(node3)  # 1 指向递归返回的链表头
        node2.next = node1  # 2 指向 1
  
        return node2  # 返回交换后的链表头节点
```
时间复杂度O(n)，空间复杂度O(n)
![](assets/24.%20Swap%20Nodes%20in%20Pairs/file-20260225212102203.png)