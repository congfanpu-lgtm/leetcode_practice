**刷题日期**：2026-2-22
**难度**：Medium

## 题目截图：
---
![](assets/438.%20Find%20All%20Anagrams%20in%20a%20String/file-20260222095515381.png)

## 参考答案：
---
```python
class Solution:
    def findAnagrams(self, s: str, p: str) -> List[int]:
        cnt_p = Counter(p)
        cnt_s = Counter()
        ans = []
  
        for right,c in enumerate(s):
            cnt_s[c] += 1
  
            left = right - len(p) + 1
            if left < 0:
                continue
            if cnt_s == cnt_p:
                ans.append(left)
  
            cnt_s[s[left]] -= 1
        return ans
```
**定长滑窗**。枚举 s 的所有长为 n 的子串 t，如果 t 的每种字母的出现次数，和 p 的每种字母的出现次数都相同，那么 t 是 p 的异位词。

```python
class Solution:
	def findAnagrams(self, s: str, p: str) -> List[int]:
		cnt = Counter(p)
		ans = []
		left = 0

		for right, x in enumerate(s):
			cnt[x] -= 1 # 如果x 不在p中 那么cnt[x] -= 1 会小于0 直到过了这一个字母之后才会继续
			while cnt[x] < 0:
				cnt[s[left]] += 1
				left += 1
		if right - left + 1 == len(p):
		ans.append(left)
	return ans
```
**不定长滑窗。** 时间复杂度小于定长滑窗