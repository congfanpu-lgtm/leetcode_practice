**刷题日期**：2026-2-22
**难度**：Easy

## 题目截图：(前缀和模版题)
---
![](assets/303.%20Range%20Sum%20Query%20-%20Immutable/file-20260222110155398.png)


## 参考答案：
---
```python
class NumArray:
    def __init__(self, nums: List[int]):
        s = [0] * (len(nums) + 1)
        for i, x in enumerate(nums):
            s[i + 1] = s[i] + x
        self.s = s
  
    def sumRange(self, left: int, right: int) -> int:
        return self.s[right + 1] - self.s[left]      


# Your NumArray object will be instantiated and called as such:

# obj = NumArray(nums)

# param_1 = obj.sumRange(left,right)
```

__init__：一次预处理，负责准备数据

sumRange：多次查询，负责高效返回结果

这种设计体现了"预处理换时间"的思想，适合查询次数多、数据不变的场景
![](assets/303.%20Range%20Sum%20Query%20-%20Immutable/file-20260222110155394.png)