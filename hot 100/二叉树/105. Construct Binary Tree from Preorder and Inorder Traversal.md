**刷题日期**：2026-2-28
**难度**：Medium

## 题目截图：
---
![](assets/105.%20Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/file-20260228084038839.png)

## 参考答案：
---
做法一
```python
class Solution:
    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:
        if not preorder:
            return None
        left_size = inorder.index(preorder[0])
        left = self.buildTree(preorder[1:1 + left_size],inorder[:left_size])
        right = self.buildTree(preorder[1 + left_size:], inorder[1+left_size:])
        return TreeNode(preorder[0],left,right)
```
时间复杂度：O(n^2 )，其中 n 为 preorder 的长度。最坏情况下二叉树是一条链，我们需要递归 O(n) 次，每次都需要 O(n) 的时间查找 preorder[0] 和复制数组。
空间复杂度：O(n^2 )
![](assets/105.%20Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/file-20260228084306265.png)

做法二（优化）：
1. 用一个哈希表（或者数组）预处理 inorder 每个元素的下标，这样就可以 O(1) 查到 preorder[0] 在 inorder 的位置，从而 O(1) 知道左子树的大小。
2. 把递归参数改成子数组下标区间（左闭右开区间）的左右端点，从而避免复制数组

```python
class Solution:
    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:
        index = {x: i for i, x in enumerate(inorder)}
        # 字典推导式（一行代码）
		# index = {x: i for i, x in enumerate(inorder)}

		# 等同于传统的for循环（多行代码）
		# index = {}
		# for i, x in enumerate(inorder):
		    # index[x] = i
        def dfs(pre_l: int, pre_r: int, in_l: int) -> Optional[TreeNode]:
        # `pre_l` (前序左边界) pre_r (前序右边界) in_l (中序左边界)
            if pre_l == pre_r:  # 空节点
                return None
            # index[preorder[pre_l]]：根节点在中序遍历中的索引位置
            left_size = index[preorder[pre_l]] - in_l  # 左子树的大小 
            left = dfs(pre_l + 1, pre_l + 1 + left_size, in_l)
            right = dfs(pre_l + 1 + left_size, pre_r, in_l + 1 + left_size)
            return TreeNode(preorder[pre_l], left, right)
  
        return dfs(0, len(preorder), 0)  # 左闭右开区间
```
- 时间复杂度：O(n)，其中 n 为 preorder 的长度。递归 O(n) 次，每次只需要 O(1) 的时间。
- 空间复杂度：O(n)
![](assets/105.%20Construct%20Binary%20Tree%20from%20Preorder%20and%20Inorder%20Traversal/file-20260228090406315.png)