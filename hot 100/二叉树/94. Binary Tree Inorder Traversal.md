**刷题日期**：2026-2-26
**难度**：Easy

## 题目截图：
---
![](assets/94.%20Binary%20Tree%20Inorder%20Traversal/file-20260226205217192.png)

## 参考答案：
---
做法一（递归）：
```python
class Solution:
    def inorderTraversal(self, root: Optional[TreeNode]) -> List[int]:
        def dfs(node: Optional[TreeNode]) -> None:
            if node is None:
                return
            dfs(node.left)       # 左
            ans.append(node.val) # 根（这行代码移到前面就是前序，移到后面就是后序）
            dfs(node.right)      # 右
  
        ans = []
        dfs(root)
        return ans
```
- 时间复杂度：O(n)，其中 n 是二叉树的节点个数。
- 空间复杂度：O(h)，其中 h 是二叉树的高度。递归需要 O(h) 的栈空间。返回值不计入。

==补充：==
二叉树有三种常见的遍历方式：

前序遍历(**Preorder Traversal**)：根-左-右。先获取根节点值，再访问根的左子树，最后访问根的右子树。
中序遍历(**Inorder Traversal**：左-根-右。先访问根的左子树，再获取根节点值，最后访问根的右子树。
后序遍历(**Postorder Traversal**)：左-右-根。先访问根的左子树，再访问根的右子树，最后获取根节点值。

做法二（线索二叉树）：
```python
class Solution:
    def inorderTraversal(self, root: Optional[TreeNode]) -> List[int]:
        ans = []
  
        while root:
            if root.left:
                # 找 root 的前驱 pre：在中序遍历中，root 的上一个节点
                # 从 root.left 开始，一直向右走，直到走到尽头，或者遇到指向 root 的线索（回到 root 的路）
                pre = root.left
                while pre.right and pre.right is not root:
                    pre = pre.right
  
                # root 的左子树尚未访问 第二张图到第三张图
                if pre.right is None:
                    pre.right = root  # 建立线索（回到 root 的路），相当于把 pre.right 当作栈
                    root = root.left  # 访问左子树
                    continue
  
                # root 的左子树访问完毕，去掉线索，恢复原样
                pre.right = None  # 注：如果调用完 inorderTraversal 不再使用这棵二叉树，这行代码可以去掉
  
            # root 的左子树访问完毕
            ans.append(root.val)  # 记录当前节点的值
            root = root.right  # 如果有右子树就访问右子树，没有就顺着线索回到指向的节点

        return ans
```
时间复杂度：O(n)，其中 n 是二叉树的节点个数。虽然写了个二重循环，但二叉树的每条边至多访问三次：第一次寻找前驱、遍历二叉树、第二次寻找前驱。所以总的循环次数是 O(n)。
空间复杂度：O(1)。返回值不计入。

方法二解析：
![](assets/94.%20Binary%20Tree%20Inorder%20Traversal/file-20260226211443298.png)
![](assets/94.%20Binary%20Tree%20Inorder%20Traversal/file-20260226211450706.png)
![](assets/94.%20Binary%20Tree%20Inorder%20Traversal/file-20260226211500028.png)
![](assets/94.%20Binary%20Tree%20Inorder%20Traversal/file-20260226211509722.png)
![](assets/94.%20Binary%20Tree%20Inorder%20Traversal/file-20260226211532118.png)