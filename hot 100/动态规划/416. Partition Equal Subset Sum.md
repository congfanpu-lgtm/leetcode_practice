**刷题日期**：2026-3-6
**难度**：Medium

## 题目截图：
---
![](assets/416.%20Partition%20Equal%20Subset%20Sum/file-20260306195121778.png)


## 回答（正确）：
---
```python
class Solution:
    def canPartition(self, nums: List[int]) -> bool:
        n = sum(nums)
        m = len(nums)
        if n % 2:
            return False
        @cache
        def dfs(i,c) ->bool:
            if i >= m or c < 0:
                return False
            if c == n/2:
                return True
            return dfs(i+1, c-nums[i]) or dfs(i+1, c) ## 只有有一种方法可以分割，那么返回正确，负责错误
        return dfs(0,n)
```
## 参考答案：
---
记忆化搜索（写法一）：
```python
class Solution:
    def canPartition(self, nums: List[int]) -> bool:
        @cache  # 缓存装饰器，避免重复计算 dfs 的结果（记忆化）
        def dfs(i: int, j: int) -> bool:
            if i < 0:
                return j == 0
            if j < nums[i]:
                return dfs(i - 1, j)  # 只能不选
            return dfs(i - 1, j - nums[i]) or dfs(i - 1, j)  # 选或不选
  
        s = sum(nums)
        return s % 2 == 0 and dfs(len(nums) - 1, s // 2)
```
写法二：
```python
class Solution:
    def canPartition(self, nums: List[int]) -> bool:
        @cache  # 缓存装饰器，避免重复计算 dfs 的结果（记忆化）
        def dfs(i: int, j: int) -> bool:
            if i < 0:
                return j == 0
            return j >= nums[i] and dfs(i - 1, j - nums[i]) or dfs(i - 1, j)
            ## 相当于 return (j >= nums[i] and dfs(i-1, j-nums[i])) or dfs(i-1, j) 因为and优先级高


        s = sum(nums)
        return s % 2 == 0 and dfs(len(nums) - 1, s // 2)
```
时间复杂度：O(ns)，其中 n 是 nums 的长度，s 是 nums 的元素和（的一半）。由于每个状态只会计算一次，动态规划的时间复杂度 = 状态个数 × 单个状态的计算时间。本题状态个数等于 O(ns)，单个状态的计算时间为 O(1)，所以动态规划的时间复杂度为 O(ns)。
空间复杂度：O(ns)。保存多少状态，就需要多少空间。

改为递推：
![700](assets/416.%20Partition%20Equal%20Subset%20Sum/file-20260306195121779.png)
```python
class Solution:
    def canPartition(self, nums: List[int]) -> bool:
        s = sum(nums)
        if s % 2:
            return False
        s //= 2  # 注意这里把 s 减半了
        n = len(nums)
        f = [[False] * (s + 1) for _ in range(n + 1)]
        f[0][0] = True
        for i, x in enumerate(nums):
            for j in range(s + 1):
                f[i + 1][j] = j >= x and f[i][j - x] or f[i][j]
        return f[n][s]
```
- 时间复杂度：O(ns)，其中 n 是 nums 的长度，s 是 nums 的元素和（的一半）。
- 空间复杂度：O(ns)

空间优化：
![700](assets/未命名/file-20260306194646323.png)
```python
class Solution:
    def canPartition(self, nums: List[int]) -> bool:
        s = sum(nums)
        if s % 2:
            return False
        s //= 2  # 注意这里把 s 减半了
        f = [True] + [False] * s
        s2 = 0
        for i, x in enumerate(nums):
            s2 = min(s2 + x, s)
            for j in range(s2, x - 1, -1):
                f[j] = f[j] or f[j - x]
            if f[s]:
                return True
        return False
```
- 时间复杂度：O(ns)，其中 n 是 nums 的长度，s 是 nums 的元素和（的一半）。
- 空间复杂度：O(s)