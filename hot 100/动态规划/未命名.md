**刷题日期**：2026-3-6
**难度**：Medium

## 题目截图：
---


## 回答（错误）：
---
```python
class Solution:
    def maxProduct(self, nums: List[int]) -> int:
        ans = 0
        n = len(nums)
        @cache
        def dfs(i:int) -> int:
            res = 1
            for j in range(i):
                for k in range(j,i):
                    res *= nums[k]
                    nonlocal ans
                    ans = max(ans,res)
        for i in range(n):
            dfs(i+1)
        return ans
```

## 参考答案：
---
```

```