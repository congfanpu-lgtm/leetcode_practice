**刷题日期**：2026-2-28
**难度**：Medium

## 题目截图：
---
![](assets/208.%20Implement%20Trie%20(Prefix%20Tree)/file-20260228214845364.png)

## 参考答案 “若我俩都走过同样的路，则我俩心意相通。”：
---
```python
class Node:
    __slots__ = 'son', 'end'
  
    def __init__(self):
        self.son = {} # 子节点字典，键是字符，值是下一个Node
        self.end = False

class Trie:
    def __init__(self):
        self.root = Node()
  
    def insert(self, word: str) -> None:
        cur = self.root
        for c in word:
            if c not in cur.son:  # 无路可走？
                cur.son[c] = Node()  # 那就造路！
            cur = cur.son[c]
        cur.end = True
  
    def find(self, word: str) -> int:
        cur = self.root
        for c in word:
            if c not in cur.son:  # 道不同，不相为谋
                return 0
            cur = cur.son[c]
        # 走过同样的路（2=完全匹配，1=前缀匹配）
        return 2 if cur.end else 1
  
    def search(self, word: str) -> bool:
        return self.find(word) == 2
  
    def startsWith(self, prefix: str) -> bool:
        return self.find(prefix) != 0
```
时间复杂度：初始化为 O(1)，insert 为 O(n∣Σ∣)，其余为 O(n)，其中 n 是 word 的长度，∣Σ∣=26 是字符集合的大小。注意创建一个节点需要 O(∣Σ∣) 的时间（如果用的是数组）。
空间复杂度：O(qn∣Σ∣)。其中 q 是 insert 的调用次数

可视化（插入ca）：
![600](assets/208.%20Implement%20Trie%20(Prefix%20Tree)/file-20260228220030932.png)
## 解题思路
![](assets/208.%20Implement%20Trie%20(Prefix%20Tree)/file-20260228215000184.png)![](assets/208.%20Implement%20Trie%20(Prefix%20Tree)/file-20260228215013832.png)