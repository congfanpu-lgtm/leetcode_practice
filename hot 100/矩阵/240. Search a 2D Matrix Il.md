**刷题日期**：2026-2-25
**难度**：Medium

## 题目截图：
----
![](assets/240.%20Search%20a%202D%20Matrix%20Il/file-20260225130430428.png)


## 参考答案：
---
```python
class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:
        m, n = len(matrix), len(matrix[0])
        i, j = 0, n - 1  # 从右上角开始
        while i < m and j >= 0:  # 还有剩余元素
            if matrix[i][j] == target:
                return True  # 找到 target
            if matrix[i][j] < target:
                i += 1  # 这一行剩余元素全部小于 target，排除
            else:
                j -= 1  # 这一列剩余元素全部大于 target，排除
        return False

```
优化普通搜索法的时间复杂度O(mn)到O(m+n)，空间复杂度O(1)


## 解题思路：
---
![](assets/240.%20Search%20a%202D%20Matrix%20Il/file-20260225131022963.png)
灵神： https://leetcode.cn/problems/search-a-2d-matrix-ii/solutions/2783938/tu-jie-pai-chu-fa-yi-tu-miao-dong-python-kytg/