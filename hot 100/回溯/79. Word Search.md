**刷题日期**：2026-3-2
**难度**：Medium

## 题目截图：
---
![](assets/79.%20Word%20Search/file-20260302105821392.png)

## 回答（错误）：
---
```python
class Solution:
    def exist(self, board: List[List[str]], word: str) -> bool:
        q = []
        m = len(word)
        n = 0
        for i, row in enumerate(board):
            for j, x in enumerate(row):
                if x == word[0]:
                    q.append((i,j))
        def dfs(i:int, j:int) -> None:
            if n == m - 1 and List[i][j] == word[n]:
                return True
            if List[i][j] == word[n]:
                return
            dfs(i+1,j)
            dfs(i,j+1)
            dfs(i-1,j)
            dfs(i,j-1)
  
        for x in q:
            dfs(x)
        return
```
思路：
1. 想模仿[994. Rotting Oranges](../图论/994.%20Rotting%20Oranges.md) 来写
2. 先寻找哪些地方有起始字母
3. 回溯中记录当前查找第几个字母

## 参考答案：
---
```

```