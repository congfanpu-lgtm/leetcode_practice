**刷题日期**：2026-3-2
**难度**：Medium

## 题目截图：
---
![](assets/79.%20Word%20Search/file-20260302105821392.png)

## 回答（错误）：
---
```python
class Solution:
    def exist(self, board: List[List[str]], word: str) -> bool:
        q = []
        m = len(word)
        n = 0
        for i, row in enumerate(board):
            for j, x in enumerate(row):
                if x == word[0]:
                    q.append((i,j))
        def dfs(i:int, j:int) -> None:
            if n == m - 1 and List[i][j] == word[n]:
                return True
            if List[i][j] == word[n]:
                return
            dfs(i+1,j)
            dfs(i,j+1)
            dfs(i-1,j)
            dfs(i,j-1)
  
        for x in q:
            dfs(x)
        return
```
思路：
1. 想模仿[994. Rotting Oranges](../图论/994.%20Rotting%20Oranges.md) 来写
2. 先寻找哪些地方有起始字母
3. 回溯中记录当前查找第几个字母

## 参考答案：
---
优化前：
```python
class Solution:
    def exist(self, board: List[List[str]], word: str) -> bool:
        m, n = len(board), len(board[0])
        def dfs(i: int, j: int, k: int) -> bool:
            if board[i][j] != word[k]:  # 匹配失败
                return False
            if k == len(word) - 1:  # 匹配成功！
                return True
            # 能到这里说明board[i][j]能找大，并且不是最后一个，可以往下走：
            board[i][j] = ''  # 标记访问过
            for x, y in (i, j - 1), (i, j + 1), (i - 1, j), (i + 1, j):  # 相邻格子
                if 0 <= x < m and 0 <= y < n and dfs(x, y, k + 1):
                    return True  # 搜到了！
            board[i][j] = word[k]  # 恢复现场
            return False  # 没搜到
        return any(dfs(i, j, 0) for i in range(m) for j in range(n))
```
![600](assets/79.%20Word%20Search/file-20260302111542541.png)

优化后：
```python
class Solution:
    def exist(self, board: List[List[str]], word: str) -> bool:
        cnt = Counter(c for row in board for c in row)
        if not cnt >= Counter(word):  # 优化一
            return False
        if cnt[word[-1]] < cnt[word[0]]:  # 优化二
            word = word[::-1]
  
        m, n = len(board), len(board[0])
        def dfs(i: int, j: int, k: int) -> bool:
            if board[i][j] != word[k]:  # 匹配失败
                return False
            if k == len(word) - 1:  # 匹配成功！
                return True
            board[i][j] = ''  # 标记访问过
            for x, y in (i, j - 1), (i, j + 1), (i - 1, j), (i + 1, j):  # 相邻格子
                if 0 <= x < m and 0 <= y < n and dfs(x, y, k + 1):
                    return True  # 搜到了！
            board[i][j] = word[k]  # 恢复现场
            return False  # 没搜到
        return any(dfs(i, j, 0) for i in range(m) for j in range(n))
```
![](assets/79.%20Word%20Search/file-20260302112336369.png)
![](assets/79.%20Word%20Search/file-20260302111655894.png)